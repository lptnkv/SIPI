<div class="buffer-top">
  <h1><%= t('app.events') %></h1>

  <div class="buffer-both">
    <%= link_to t('controllers.events.actions.create_event'), new_event_path, class: 'btn btn-success' %>
  </div>

  <div class="row buffer-button">
    <%= render partial: 'events/event', collection: @events %>
  </div>
</div>
